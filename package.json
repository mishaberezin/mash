{
  "name": "mash",
  "version": "0.1.0",
  "scripts": {
    "start": "parcel src/stand.html --open --no-hmr --https",
    "build": "parcel build src/stand.html",
    "deploy": "echo todo",
    "test": "echo todo"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.3",
    "parcel-plugin-svelte": "^3.0.1",
    "svelte": "^3.6.1",
    "normalize.css": "^8.0.1"
  },
  "browserslist": [
    "last 1 chrome versions"
  ],
  "dependencies": {
    "svelte-fragment": "^1.0.1"
  }
}
