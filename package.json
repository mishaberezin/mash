{
  "name": "mash",
  "version": "0.1.0",
  "scripts": {
    "start": "parcel src/*.html src/stands/*.html --open --no-hmr --https --log-level 2",
    "build": "parcel build src/*.html src/stands/*.html --experimental-scope-hoisting",
    "prebuild": "rm -rf ./dist",
    "deploy": "npm run build && surge ./dist --domain layoutmash.surge.sh",
    "test": "echo todo"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.3",
    "parcel-plugin-svelte": "^3.0.1",
    "svelte": "^3.6.1",
    "normalize.css": "^8.0.1"
  },
  "browserslist": [
    "last 1 chrome versions"
  ],
  "dependencies": {
    "sections": "^1.0.0",
    "svelte-fragment": "^1.0.1"
  }
}
